import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.felipe.wallpaperapp',
  appName: 'My_wallpaper_app',
  webDir: 'www'
};

export default config;
